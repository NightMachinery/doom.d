;;; ~/doom.d/autoload/night-org.el -*- lexical-binding: t; -*-

(setq org-image-actual-width 600) ; this zooms small images though and downscales big ones. It unfortunately overrides per-image attribute settings.
