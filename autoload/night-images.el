;;; autoload/night-images.el -*- lexical-binding: t; -*-

;;;
;; (add-hook 'find-file-hook 'night/icat)
;; (defun night/icat ()
;;   (when (string= (file-name-extension buffer-file-name) "png")
;;     (night/yank-buffer-filename)))
;;;
