# -*- mode: snippet -*-
# name: llm-default
# key: <l
# uuid: <l
# --
#+begin_src jupyter-python :kernel py_base :session llm_1 :async yes :exports both
res = openai_chat_complete(
    ###
    # model="gpt-4o",
    # model="OR:anthropic/claude-4-opus",
    # model="OR:anthropic/claude-4-sonnet",
    # model="OR:x-ai/grok-4",
    ##
    # model="OR:google/gemini-2.5-pro",
    # model="GAI:gemini-2.5-pro",
    model="GAI:gemini-flash-latest",
    ###
    messages=[
        # {"role": "system", "content": """Output your changes in the unified diff format. When appropriate, refactor so that you don't repeat yourself (DRY)."""},
        {"role": "user", "content": r"""
`%`$0
        """},
    ],
    temperature=0,
    max_tokens=8000,
    interactive=True,
)

print_chat_streaming(res, copy_mode=None) # "chat2"
bell_gpt()
#+end_src
